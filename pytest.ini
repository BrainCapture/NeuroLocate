[pytest]
testpaths = tests
# `tests/` imports the app package (`neurolayout`) and its own `conftest`
# helpers by name, and the component `tesseract_api.py` files are loaded by path.
pythonpath = app tests
addopts = -ra --strict-markers
filterwarnings =
    ignore::DeprecationWarning
